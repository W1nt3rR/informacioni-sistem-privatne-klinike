import { Routes } from '@angular/router';
import { SpecializationListComponent } from './specialization-list.component';

export const SPECIALIZATION_ROUTES: Routes = [
  { path: '', component: SpecializationListComponent },
];
